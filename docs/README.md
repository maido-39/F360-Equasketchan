# Spec — reading order for implementation

Read BOTH, in order, before writing code:

1. `SPEC_features.md`  (v0.1) — WHAT to build: functional requirements
   (input modes, function library, parameter linkage, output, UI, acceptance).
2. `SPEC_implementation.md` (v0.2) — HOW to build on Fusion's reality + how the
   agent verifies/executes: platform constraints, architecture, re-edit
   milestones, dev env, agent test harness, security, remote Windows topology,
   **§24 research-refresh checklist**, and **§25 execution runbook + DoD**.

Then: run §24 (log deltas in `CHANGES.md`) → follow §25 step by step.
